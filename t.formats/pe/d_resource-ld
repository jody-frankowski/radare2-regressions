#!/bin/sh
for a in . .. ../.. ; do [ -e $a/tests.sh ] && . $a/tests.sh ; done

NAME='PE: corkami d_resource-ld.exe - open'
FILE=../../binaries/pe/d_resource-ld.exe
ARGS=''
CMDS="q!"
EXPECT=''
run_test